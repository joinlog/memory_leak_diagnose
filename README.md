# memory_leak_diagnose
replace C API malloc calloc realloc with defined functions;
then it will statistics the memory usage in these functions;
when the programe end, we will see the memory not be freed.
